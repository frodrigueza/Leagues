<%= render layout:'version' do %>


	<h1><%= @version.name %></h1>

	<!-- equipos inscritos -->
	<h2>Equipos inscritos</h2>
		<div class="row">
			<div class="col-md-12">
				<% @version.inscriptions.each do |i| %>
					<tr>
						<td><%= i.team.name %></td>
					</tr>
				<% end %>
			</div>
		</div>

	<!-- equipos inscritos -->
	<h2>Fases</h2>
		<div class="row">
			<div class="col-md-12">
				<% @version.inscriptions.each do |i| %>
					<tr>
						<td><%= i.team.name %></td>
					</tr>
				<% end %>
			</div>
		</div>


<% end %>